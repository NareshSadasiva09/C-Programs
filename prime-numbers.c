#include<stdio.h>
int main () {
    int n, i = 1, c = 0;
    scanf("%d", &n);
    while(i <= n) {
        if(1) {
            // TODO : Generate a Prime number in between given two inputs
        }
        i++;
    }
    printf("\n");
}