#include<stdio.h>
int main() {
    int n, socks_to_remove;
    scanf("%d", &n);
    socks_to_remove = n+1;
    printf("Total number of socks to be removed to get matching pair = %d", socks_to_remove);
    return 0;
}
